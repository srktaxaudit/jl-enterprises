package in.jlenterprises.ecommerce.constant;

/** The four fixed roles. Stored as strings; used as Spring Security authorities. */
public enum RoleName {
    ROLE_SUPER_ADMIN,
    ROLE_ADMIN,
    ROLE_MANAGER,
    ROLE_CUSTOMER
}
