package in.jlenterprises.ecommerce.constant;

public enum CouponType {
    /** value is a percentage (0–100) of the eligible subtotal. */
    PERCENTAGE,
    /** value is a flat amount in the order currency. */
    FIXED
}
