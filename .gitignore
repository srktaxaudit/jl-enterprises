# ── Dependencies ──
/node_modules
mobile/node_modules
mobile-store/node_modules

# ── Build output ──
/.next
/out
/build
ecommerce-backend/target/
next-env.d.ts
*.tsbuildinfo

# ── Expo / React Native (mobile/, mobile-store/) ──
.expo/
mobile/.expo/
mobile-store/.expo/

# ── Deploy / local ──
.vercel
.env
.env*.local
# .env.example is TRACKED on purpose — placeholders only, never real values.
!.env.example

# ── Claude Code ──
# Machine-specific state (local permission grants with absolute paths) — never shared.
# .claude/launch.json IS tracked: it is the shared dev-server config for the team.
.claude/settings.local.json
.claude/settings.local.json.bak

# ── OS / editor ──
.DS_Store
Thumbs.db
npm-debug.log*
.vscode/
.idea/
*.iml

# ── Secrets & local data (never commit) ──
*.pem
*.key
*.keystore
*.p12
Projects.xlsx
